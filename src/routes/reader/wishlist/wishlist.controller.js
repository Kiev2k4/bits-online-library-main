export const addToWishlist = (req, res) => {
	// Add a book to a user’s wishlist
};

export const removeFromWishlist = (req, res) => {
	// Remove a book from a user’s wishlist
};

export const getWishlist = (req, res) => {
	// Retrieve a list of a user’s wishlist books
};
